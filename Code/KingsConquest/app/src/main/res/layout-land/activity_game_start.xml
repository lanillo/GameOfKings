<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/background"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/home_village"
    tools:context="com.gok.luisanillo.kingsconquest.GameStart">

    <!-- The primary full-screen view. This can be replaced with whatever view
         is needed to present your content, e.g. VideoView, SurfaceView,
         TextureView, etc. -->

    <ImageView
        android:id="@+id/next"
        android:layout_width="69dp"
        android:layout_height="56dp"
        android:layout_marginBottom="8dp"
        android:layout_marginRight="8dp"
        android:contentDescription="@string/FrontArrow"
        android:onClick="next"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:srcCompat="@drawable/frontarrow"
        tools:ignore="RtlHardcoded" />

    <TextView
        android:id="@+id/history_text"
        android:layout_width="246dp"
        android:layout_height="130dp"
        android:layout_marginLeft="16dp"
        android:layout_marginTop="16dp"
        android:maxLines="5"
        android:scrollbars="vertical"
        android:text="@string/History"
        android:textAppearance="@android:style/TextAppearance.Holo.Widget.EditText"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="RtlHardcoded" />

    <!-- This FrameLayout insets its children based on system windows using
         android:fitsSystemWindows. -->


</android.support.constraint.ConstraintLayout>
